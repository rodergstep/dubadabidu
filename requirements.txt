# DERIVED FILE — pyproject.toml is the source of truth for dependencies.
# Kept only because `doctor` and the README hand it to a human who wants one
# copy-pasteable line. If you change a pin, change it in pyproject.toml and
# mirror it here; tests/test_deps.py asserts the two agree.
#
# Rewritten 2026-08-03. The previous version was the CHATTERBOX-ERA file and
# had gone actively harmful: it installed chatterbox-tts==0.1.7 — an engine
# removed from this repo on 2026-08-02 — whose hard pin of torch/torchaudio
# 2.6.0 would have DOWNGRADED the 2.11.0 the pod bootstrap installs, and
# audio-separator 0.30.2, three minor versions behind the pyproject pin, held
# back by a numpy<2 ceiling that left with chatterbox. Following `doctor`'s own
# hint would have broken the environment.
#
# Preferred install (gets the extras and the console script):
#     pip install -e '.[dev,sep]'          # add ,mac on Apple Silicon
#
# torch/torchaudio are deliberately ABSENT here as well as in pyproject: the pod
# bootstrap installs 2.11.0 from the cu128 index first, and audio-separator
# declares torch>=2.3 with no upper bound, so an unpinned resolve pulls 2.13 and
# breaks the torchaudio pairing (torchaudio stopped at 2.11). Locally, install
# them on the SAME command line as the separation extra:
#     pip install 'audio-separator[cpu]==0.44.5' torch==2.11.0 torchaudio==2.11.0

faster-whisper==1.2.1
edge-tts==7.2.8          # fallback voices, no cloning
srt==3.5.3
soundfile==0.14.0
librosa==0.11.0
pydub==0.25.1
openai==2.52.0           # universal client: OpenAI / DeepSeek / Ollama via base_url
jiwer                    # WER for qc/backcheck
num2words                # digit->word expansion in backcheck WER normalization
ruaccent==1.5.8.3        # RU lexical stress marking (tts.ru_stress)
speechbrain==1.1.0       # ECAPA-TDNN speaker similarity (qc), Apache-2.0
distillmos==0.9.1        # neural MOS naturalness proxy (qc), MIT (Microsoft)
pyyaml

# --- extras (pyproject optional-dependencies), install only what you need ---
# .[sep]    audio-separator[cpu]==0.44.5 + demucs==4.1.0   — s1, LOCAL ONLY
# .[mac]    mlx-whisper                                    — Apple Silicon ASR
# .[master] pedalboard (GPLv3)                             — mix.master.enabled
# .[dev]    pytest
# optional, tighter word-level subtitle timing:
# whisperx==3.8.6
